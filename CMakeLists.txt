cmake_minimum_required(VERSION 3.28.3)
project(tcptest1)
set(SRCPATH ${PROJECT_SOURCE_DIR}/src)
file(GLOB SRC ${SRCPATH}/*.cpp)
set(EXEPATH ${PROJECT_SOURCE_DIR}/bin)
set(EXECUTABLE_OUTPUT_PATH ${EXEPATH})
# include_directories()
add_executable(return_same ${SRC})